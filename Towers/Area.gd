extends Area


# Declare member variables here. Examples:
# var a = 2
# var b = "text"
var x = false
var newPos

# Called when the node enters the scene tree for the first time.

#func _process(delta):
	#if x:
		#newPos = get_node("../Tower").translation
		#translation = newPos
	
